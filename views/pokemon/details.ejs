


<h3><%= pokemon.name %></h3>

<form action="/comments/<%= pokemon.id %>/new" method="POST">

    <input type="text" name="description" autocomplete="off">

    <button type="submit">Add comment</button>
</form>

 <% pokemon.comments.forEach(c => { %>
        <p>
            <%= c.description %>
        </p>
<% }) %>
